# blog-bot 部署指南

从零把这套路由跑起来：在 QQ（**首选通道**）里发消息 / 发图 → 机器人拼 Markdown → 提交 GitHub 触发博客部署。

> 配套阅读：项目结构、指令集、路由总表见 [README.md](./README.md)。本文只讲「怎么部署」和「踩过的坑」。
>
> 文中的敏感值（Token/Secret/PAT）一律用占位符，真实值只存在 Cloudflare / EdgeOne 的环境变量里，不进任何文件。
>
> **已移除企业微信通道与 R2 存储**：图片走自建图床 `imgbed.yufish.cn`，友链只写 check-flink GitHub 真源，不再依赖 R2。

---

## 1. 前置清单

| 需要 | 用途 | 备注 |
| --- | --- | --- |
| Cloudflare 账号 | Worker + KV（cf 模式必需） | 免费额度够用 |
| Node.js ≥ 18 + wrangler CLI | 本地部署工具 | `npm i -g wrangler` 或直接 `npx wrangler` |
| QQ 开放平台账号 | QQ 机器人接入（**首选通道**） | 第 5 节 |
| GitHub PAT | 写文章仓库、写友链真源、开友链申请 Issue | 第 2.2 节建 |
| EdgeOne（可选） | eo 模式的国内入口 | 第 10、11 节，纯 cf 模式用不到 |

先把代码拉下来（依赖仅 cf 路径的 Ed25519 用得到）：

```bash
git clone <本仓库>
cd blog-bot
npm install        # @noble/ed25519 + @noble/hashes，仅 cf 模式 QQ Webhook 签名用
node --check worker.js && node --check "edge-functions/[[default]].js"   # 语法预检
```

---

## 2. Cloudflare 资源准备（cf 模式）

### 2.1 KV 命名空间（存草稿 + 缓存 token）

控制台：Workers & Pages → KV → Create a namespace → 名字随意（如 `blog-bot-drafts`）。

把生成的 **ID** 填进 `wrangler.toml`：

```toml
[[kv_namespaces]]
binding = "DRAFTS"     # 必须叫 DRAFTS，代码里按这个名字取
id = "<你的KV命名空间ID>"
```

KV 里存的东西：博文/动态草稿、QQ `access_token` 缓存、最近单聊用户 openid。

> 不再有 R2 绑定。图片走图床、友链走 GitHub，KV 只承担草稿与 token 缓存。

### 2.2 GitHub PAT

classic token 勾 `repo` 全组即可（私有仓库必须）；fine-grained 的话给两个仓库授权：

| 仓库 | 权限 | 干什么 |
| --- | --- | --- |
| `my-blog`（`GH_OWNER`/`GH_REPO`） | Contents 读写 | `/done` 提交文章、动态 |
| `check-flink`（`FL_*`） | Contents 读写 | `/友链` 更新真源 `friends.json` |

建好先别到处贴，第 3 节 `wrangler secret put GH_TOKEN` 时用。

---

## 3. 变量与密钥

### 3.1 `[vars]`（非敏感，明文写在 wrangler.toml）

当前已配好的不用动，换人部署时核对这张表：

| 变量 | 说明 | 当前值 |
| --- | --- | --- |
| `GH_OWNER` / `GH_REPO` | 博客仓库 | `ImYufish` / `my-blog` |
| `GH_BRANCH` | 提交分支 | `master` |
| `GH_PATH` | 文章目录 | `src/content/posts` |
| `GH_DYNAMIC_PATH` | 动态目录 | `src/content/dynamic` |
| `SITE_URL` | 博客地址（不带末尾斜杠） | `https://x1anyu.cn` |
| `FILENAME_FORMAT` | 文件名格式：`title`=纯标题 / `date`=带日期前缀。动态始终时间戳 | `title` |
| `DEPLOY_MODE` | `cf` / `eo`，见第 10 节 | 当前 `cf` |
| `FRIEND_PAGE_URL` | `/友链` 添加成功后回显的浏览链接 | `https://x1anyu.cn/friends/` |
| `WALINE_AUTHOR_MAIL` | 跳过博主自己评论的邮箱（精确匹配评论 mail） | `blog@x1anyu.cn` |
| `FL_OWNER` / `FL_REPO` / `FL_BRANCH` / `FL_PATH` | 友链真源仓库定位 | `ImYufish` / `Friend-Circle-Lite` / `main` / `friends.json` |
| `QQ_APPID` | QQ 机器人 AppID（非敏感才放这） | 见 wrangler.toml |
| `QQ_CALLBACK_PATH` | QQ 回调路由，和运行时一致 | `/qq/callback` |
| `IMG_BED_URL` | 图床地址 | `https://imgbed.yufish.cn` |
| `IMG_BED_CHANNEL` | 图床上传渠道；留空=实例默认，报 channel 错再填 | （空） |

### 3.2 secret（敏感，只进运行环境，逐条执行、按提示粘贴）

```bash
wrangler login

# ---- QQ 机器人（首选通道，必配）----
wrangler secret put QQ_APP_SECRET  # 开放平台「开发管理」的 AppSecret（只有一个，无独立 BotSecret）

# ---- GitHub（必配）----
wrangler secret put GH_TOKEN       # 第 2.2 节的 PAT

# ---- QQ 告警推送（要用 /api/alert 才配）----
wrangler secret put ALERT_TOKEN    # /api/alert 与 /api/waline 共用的鉴权 token，自定随机串
wrangler secret put QQ_OWNER_OPENID # 告警推送目标，获取方式见第 8 节，可以先跳过

# ---- 图床（要发图 / 用 /删图 才配）----
wrangler secret put IMG_BED_TOKEN  # 图床 API Token，需 upload + delete 双权限（Authorization: Bearer）
# 或二选一：
wrangler secret put IMG_BED_AUTH   # 图床上传认证码 authCode，作为 IMG_BED_TOKEN 的兜底
```

注意：`wrangler secret put` 之后要 `wrangler deploy` 一次才对线上生效（secret 是跟着 deployment 走的）。

---

## 4. 部署 Worker（cf 模式）

```bash
wrangler deploy
```

成功后会输出 Worker 地址，默认是 `https://blog-bot.<你的子域>.workers.dev`。**QQ 回调地址 = 它 + `/qq/callback`**；所有诊断端点同样挂在这里。

要挂自定义域（比如裸 workers.dev 不稳时）：CF 控制台 → Workers & Pages → blog-bot → Settings → Domains & Routes → Add → Custom domain。域名 DNS 得在这个 CF 账号下。当前线上自定义域为 `bot.yufish.cn`（已在 `wrangler.toml` 的 `routes` 里，部署时会用它覆盖远程配置，不要删）。

---

## 5. QQ 机器人接入（首选通道，Webhook 模式）

业务逻辑零改动——`worker.js`（cf）与 `edge-functions/[[default]].js`（eo，EdgeOne Makers）都内联了 QQ 适配层，收到 C2C 单聊事件包成统一消息格式直接进 `dispatch`，写博文 / 友链 / 草稿流程完全一致。

1. [QQ 开放平台](https://q.qq.com/) 创建机器人，拿 AppID / AppSecret（都在「开发管理」）。
2. **千万别填 IP 白名单**。平台未配白名单时所有来源 IP 都能调 OpenAPI，Worker / EO 动态出口正好能用；一旦手贱填了，出口就被挡死了。
3. 机器人管理页 → Webhook 配置：回调地址填 `https://<域名>/qq/callback`（路径和 `QQ_CALLBACK_PATH` 一致），勾选 **C2C 单聊消息** 事件。保存时平台发 op13 握手，代码会 Ed25519 签名应答，能保存即通。
4. `QQ_APPID` 已在 vars 里，再 put 一个 secret：`wrangler secret put QQ_APP_SECRET`，然后 `wrangler deploy`。
5. （一次性）设置单聊底部菜单和指令面板，对所有用户生效：

```bash
curl https://<域名>/__qqmenu    # PUT /v2/menu
curl https://<域名>/__qqpanel   # POST /v2/panels
```

---

## 6. 验证与排障

真人肉测最稳：在 QQ 里给机器人发 `/帮助` → `/部署模式` → `/状态` → 随便一段文字（或发张图）→ `/预览` → `/done`，去 my-blog 仓库看 commit、等博客构建。

不碰线上也能验签算法：

```bash
node test-ed25519.cjs   # 纯 JS Ed25519 对 RFC 8032 测试向量校验（cf/eo 同源）
node test-qq-ed.mjs     # QQ Ed25519 签名往返（CF @noble 路径）
```

常见翻车点：

- 保存回调配置报错 → 八成是 AppSecret/AppID 与开放平台不一致，或签名校验失败；看 CF / EO 实时日志（`wrangler tail` 或 EO 日志面板）；
- `/done` 报 github 401/404 → PAT 过期或没勾 repo 权限；
- 机器人完全无反应 → 先看应用可见范围，再看实时日志；
- 「时灵时不灵」的回调 → 多半是 EdgeOne 缓存了 `/qq/callback` 回包，见 §10.4 加 bypass。

---

## 7. 告警推送接口（CI 用，如 FCL 构建告警）

给外部系统一个「只认 token、不碰 QQ 凭据」的单聊推送口：

```
POST /api/alert
{"token": "<ALERT_TOKEN>", "text": "告警文本"}
```

- text 最长 2000 字符，超长截断；token 错返回 401。
- 走 QQ **主动消息**（不带 msg_id），对方在 QQ 里关掉「允许主动发送」就会失败，失败原因原样返回。
- CI 侧只需配两个环境变量：`QQ_BOT_ALERT_URL=https://<域名>/api/alert`、`QQ_BOT_ALERT_TOKEN=<同 ALERT_TOKEN>`。

openid 配置流程（首次）：

```bash
# 1. 先用你自己的 QQ 给机器人发一条私聊（任意内容），C2C 事件会把 openid 记进 KV
# 2. 查询：
curl "https://<域名>/__qqwho?token=<ALERT_TOKEN>"
# → {"ok":true,"owner_configured":false,"last_openid":"XXXX"}
# 3. 把 last_openid put 进去并重新 deploy：
wrangler secret put QQ_OWNER_OPENID
wrangler deploy
```

---

## 8. 友链与图床（均不走 R2）

### 友链 → check-flink 真源

`/友链` 数据**只写 check-flink 真源 GitHub 仓库**（`FL_*`），不再读写 R2：

- 写入后约 1~2 分钟，博客经 `friends.yufish.cn` 实时拉取即生效，零部署、不耗部署次数；
- 友链未填头像用代码内自包含占位 SVG（data-URI），不再依赖外部图床域名。

### 图片 → 图床

QQ 发图 → 运行时拉图字节 → 上传自建图床 `imgbed.yufish.cn` → 拿 `publicUrl` 塞进草稿 `images`，`/done` 时随文章带出；`/删图` 在图床配了 token 时同步调图床删除接口真删文件。图床凭据为 `IMG_BED_TOKEN`（需 upload + delete 双权限）或可选 `IMG_BED_AUTH`（authCode 兜底），二选一。图床本身（`imgbed.yufish.cn`）是另一个独立项目，不在本仓库。

---

## 9. 两种部署模式与切换（cf / eo）

同一套业务逻辑，靠 `DEPLOY_MODE` 切两种形态（**已移除 hybrid**）：

| 模式 | 入口 | 谁干活 | 草稿存哪 | 适用 |
| --- | --- | --- | --- | --- |
| `cf` | Cloudflare Worker | `worker.js` + `src/shared.js` | CF KV（`DRAFTS`） | 完全用 Cloudflare（当前默认） |
| `eo` | EdgeOne Makers | `edge-functions/[[default]].js` | Makers KV（`DRAFTS`） | 完全用 EdgeOne，自己扛 QQ 回调 |

各文件分工：

- `worker.js`（引用 `src/shared.js`）：承担 `cf`（自己 Ed25519 验签 + 回包 + 写 KV）；
- `edge-functions/[[default]].js`：EdgeOne Makers 边缘函数，承担 `eo`（全量独立处理；`onRequest(context)` 入口，Ed25519/业务层全部内联，无 npm import）。

切换步骤（cf ↔ eo）：

1. 改对应运行侧的环境变量 `DEPLOY_MODE`（Worker 侧改 `wrangler.toml` 后 `wrangler deploy`；Makers 侧改项目环境变量后重新部署）；
2. QQ 开放平台回调 URL 指向新入口（cf → `https://<worker域名>/qq/callback`；eo → `https://<加速域名>/qq/callback`），保存触发一次 op13 握手；
3. 发 `/部署模式` 确认当前模式、`/帮助` 验证通了再切流量。代码里内置了 `/部署模式` 指令可直接问机器人。

一个坑：

- **草稿不跟随迁移**。cf 的草稿在 CF KV，eo 在 EO KV，切换模式等于换了个草稿箱。切换前先 `/done` 发布或接受草稿丢弃。

---

## 10. EdgeOne 侧配置（eo 模式）

纯 cf 模式本节可整体跳过。

### 10.1 部署到 EdgeOne Makers

eo 模式用 **EdgeOne Makers**（原 EdgeOne Pages）部署，不再用旧版 EO 控制台的「边缘函数」粘贴入口——Makers 的 Edge Functions 用 `export default function onRequest(context)`，**不支持** `addEventListener('fetch')`。两种上线方式：

- **控制台导入 Git 仓库（推荐）**：EdgeOne Makers → 创建项目 → 导入本 Git 仓库。Makers 自动识别 `edge-functions/[[default]].js` 作为全站函数入口（catch-all 路由，承载 `/qq/callback`、`/health`、`/api/*` 等全部路径），无需逐个配触发规则。
- **CLI**：`npm i -g edgeone` → 仓库根目录执行 `edgeone makers deploy`。根目录 `edgeone.json` 已声明无构建步骤（纯 edge-functions 部署），避免仓库无 `build` 脚本导致部署失败。

### 10.2 函数环境变量与绑定

Makers 侧需要的变量与 Worker 同名同义（代码是同一套读法）。在 Makers 项目「设置 → 环境变量」里加（环境变量与 KV 绑定都进这里）：

- vars：`QQ_APPID`、`QQ_CALLBACK_PATH`、`SITE_URL`、`GH_*` 系列、`FILENAME_FORMAT`、`DEPLOY_MODE`、`FRIEND_PAGE_URL`、`WALINE_AUTHOR_MAIL`、`FL_*`、`IMG_BED_URL`、`IMG_BED_CHANNEL`；
- secrets：`QQ_APP_SECRET`、`GH_TOKEN`、`ALERT_TOKEN`、`QQ_OWNER_OPENID`、`IMG_BED_TOKEN`（或 `IMG_BED_AUTH`）；
- KV 绑定：名字 `DRAFTS`（eo 模式存草稿必需）。在 Makers「KV 存储」创建命名空间后，于项目 KV 绑定处绑到 `DRAFTS`，代码里经 `context.env.DRAFTS` 访问。

改完重新部署（Git 推送自动触发，或 CLI `edgeone makers deploy`）。

### 10.3 QQ 回调指向

QQ 开放平台的 Webhook 回调 URL 改成 Makers 域名 + `/qq/callback`（`https://<Makers域名>/qq/callback`），保存触发 op13 握手验证。

### 10.4 节点缓存 bypass 规则（关键）

EdgeOne Makers 默认会按自己的策略缓存回包，哪怕源头带了 `no-store` 有时也照缓存。以下路径必须在 EdgeOne Makers 控制台「节点缓存规则」里显式设为 **bypass / 不缓存**：

| 路径 | 为什么不能缓存 |
| --- | --- |
| `/qq/callback` | QQ op13 签名每请求必不同，命中旧签名 → 平台判失败，未命中才成功，表现「时灵时不灵」 |
| `/api/alert` `/api/waline` `/__qqwho` `/__qqmenu` `/__qqpanel` | 全是实时交互 / 一次性设置 |

规则配置成精确路径匹配即可。判断是不是缓存在捣鬼的土办法：把回调地址临时改成直连地址（Worker 的 `*.workers.dev`，或 EO 函数的调试域名），立刻稳定了，那就是缓存的问题，回头把 bypass 补上。

---

## 11. Waline 评论通知桥接（复用 QQ 推送）

Waline 自带 `WEBHOOK` 环境变量：每次新评论会向该地址 POST 一条评论 JSON。我们直接把它指向 blog-bot 的 `/api/waline`，由运行时格式化成文本、复用 QQ 推送推到博客主 QQ——**不依赖 Qmsg 酱等第三方中转**，QQ 凭据始终只在运行时里。

前提：已配置 `ALERT_TOKEN`（§3.2）和 `QQ_OWNER_OPENID`（§7 的 openid 流程）。

Waline 侧（在你的 Waline 服务端环境变量里加一行）：

```bash
WEBHOOK=https://<域名>/api/waline?token=<ALERT_TOKEN>
SITE_NAME=临渊羡鱼                 # 可选，通知里显示的站名；不配默认「博客」
SITE_URL=https://blog.x1anyu.cn    # 可选，部分 Waline 版本会带进 payload 的 url 字段
```

运行时侧**无需改代码**（端点已在 `worker.js` / `edge-functions/[[default]].js` 里），但首次要 `wrangler deploy`（cf）或在 Makers 重新部署（eo）让新路由生效。

行为细节：

- 复用 `ALERT_TOKEN` 做鉴权（token 放 URL query，Waline 原样 POST 过来），不新增 secret；
- payload 防御式解析：评论对象可能是 `data.comment`、直接 `comment` 或整个 body，都能取到 `nick` / `comment` / `url` / `status`；
- 通知文本最长 2000 字，超长截断；
- 可选跳过博主自己的评论：配 `WALINE_AUTHOR_MAIL`（与 Waline 的 `AUTHOR_EMAIL` 同值）即跳过该邮箱的评论，避免自己评论也响铃；
- 走 QQ **主动消息**，对方在 QQ 关掉「允许主动发送」会失败，失败原因原样返回给 Waline。

如果想走 Waline 原生 QQ 而非我们的机器人：在 Waline 侧配 `QMSG_KEY` + `QQ_ID`（Qmsg 酱，需去 qmsg.zendee.cn 注册并把你的 QQ 加进列表），完全不碰本仓库。两边二选一即可——**推荐走我们自己的 bot**：凭据可控、不依赖第三方可用性，也契合「QQ 机器人是首选通道」的定位。
